{
  "name": "ftbquestssync-web-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only web viewer + admin reset console for ftb-quests-mysql-sync. Reads the same MySQL schema the mod writes.",
  "main": "routes.js",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.19.0",
    "ioredis": "^5.3.2",
    "mysql2": "^3.9.0",
    "prismarine-nbt": "^2.5.0"
  }
}
